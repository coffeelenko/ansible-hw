---
- name: Install Vector
  block:
    - name: Create Vector directories
      ansible.builtin.file:
        path: "{{ vector_install_dir }}"
        state: directory
        mode: '0755'

    - name: Download Vector package
      ansible.builtin.get_url:
        url: "{{ vector_base_url }}"
        dest: "/tmp/{{ vector_archive_name }}.tar.gz"
        mode: '0644'
        timeout: 30

    - name: Extract Vector archive
      ansible.builtin.unarchive:
        src: "/tmp/{{ vector_archive_name }}.tar.gz"
        dest: "{{ vector_install_dir }}"
        remote_src: true
        creates: "{{ vector_install_dir }}/vector-x86_64-unknown-linux-gnu/bin/vector"

    - name: Create symbolic link to Vector binary
      ansible.builtin.file:
        src: "{{ vector_install_dir }}/vector-x86_64-unknown-linux-gnu/bin/vector"
        dest: /usr/local/bin/vector
        state: link
        force: true

    - name: Verify Vector installation
      ansible.builtin.command: "/usr/local/bin/vector --version"
      register: vector_version_check
      changed_when: false

    - name: Create Vector data directory
      become: true
      ansible.builtin.file:
        path: /var/lib/vector
        state: directory
        mode: '0755'
        owner: root
        group: root

    - name: Create Vector config directory
      ansible.builtin.file:
        path: /etc/vector
        state: directory
        mode: '0755'

    - name: Deploy Vector configuration
      ansible.builtin.template:
        src: vector.yaml.j2
        dest: /etc/vector/vector.yaml
        mode: '0644'
      notify: Restart vector service

    - name: Create Vector systemd service
      ansible.builtin.template:
        src: vector.service.j2
        dest: /etc/systemd/system/vector.service
        mode: '0644'
      notify: Restart vector service

    - name: Ensure Vector service is running
      ansible.builtin.service:
        name: vector
        state: started
        enabled: true
